import styled from "styled-components";

const CheckSchedules = styled.div`
    .label-schedule {
        > p {
            margin-bottom: 4px;

            &:last-child {
                margin-bottom: 0;
            }
        }
    }    
`;

export default CheckSchedules;